/*
Functions with parameters
Coding conventions: functions starting with a lowercase letter 
and use camel case with no underscores.
 */

fun sumTest(x: Int, y: Int): Int {
    return x + y
}

fun main() {
    println(sumTest(1, 2))
    // 3
}